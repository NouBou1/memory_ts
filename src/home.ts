/**
 * Entry point of the start page (`index.html`).
 *
 * The start page needs no behaviour – the import merely pulls in the stylesheet
 * so that Vite bundles it for this page.
 *
 * @packageDocumentation
 */

import './styles/styles.scss';
