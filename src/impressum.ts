/**
 * Entry point of the legal notice page (`impressum.html`).
 *
 * A plain text page with no behaviour – the import merely pulls in the
 * stylesheet so that Vite bundles it for this page.
 *
 * @packageDocumentation
 */

import './styles/styles.scss';
